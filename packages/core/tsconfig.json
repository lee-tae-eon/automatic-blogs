{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": false, // 👈 true에서 false로 변경
    "declaration": true,
    "outDir": "./dist",
    "rootDir": "./src", // 👈 명시적으로 src 지정
    "module": "CommonJS", // 👈 Base에서 오지만 명시하면 안전합니다.
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
